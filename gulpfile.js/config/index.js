var config = {};

config.source = "./src";
config.project = config.source;
config.projectAssets = config.project + "/assets/";
config.projectStatic = config.project + "/static/";
config.projectCollectStatic = config.projectStatic;

module.exports = config;
